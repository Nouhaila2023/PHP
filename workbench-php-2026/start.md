sudo podman-compose --file podman-compose.yml up -d
o si estas dentro de la carpeta en la que tienes el podman-compose.yml puedes usar directamente:
    sudo podman-compose up -d
    
sudo podman-compose down <- detiene todos los contenedores cuando termines de trabajar
